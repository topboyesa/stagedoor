<?php
// This is deliberately simple: you're the only admin, so a single shared
// password is enough for now rather than building a full admin-accounts
// system. Change this to something only you know before deploying anywhere
// public, and don't commit this file (see .gitignore).

define('ADMIN_PASSWORD', 'change-this-password');
